#include <iostream>
using namespace std;

int binary_search_asc(int arr[], int size, int target)
{
    int left = 0;
    int right = size - 1;

    while (left <= right)
    {
        int mid = (left + right) / 2;

        if (arr[mid] == target)
        {
            return mid;
        }
        else if (arr[mid] < target)
        {
            left = mid + 1; // Go to  the right half
        }
        else
        {
            right = mid - 1; // Go to the left half
        }
    }

    return -1; //  not found
}
